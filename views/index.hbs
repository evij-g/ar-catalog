<h1>AR-CATALOG</h1>

{{#each allElements}}
  <div class="ctn-element">
    <p>title: {{title}}</p>
    <img src="{{imageUrl}}" alt=`picture of {{title}}` />
    <p>height: {{height}}</p>
    <p>width: {{width}}</p>
    <p>material: {{material}}</p>
    <br />
  </div>
{{/each}}

<a href="/login">login</a>
